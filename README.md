# startml_final_project
Final project for startML cources from karpov cources.

## Start program

- Deploy the environment from `requirements.txt` to the `venv` folder;
- Run server with `uvicorn app:app`.